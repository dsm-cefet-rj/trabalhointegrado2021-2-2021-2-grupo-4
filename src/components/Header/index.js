import React from 'react'
import './styled.scss'

const Header = () => {
  return(
    <div className="menu_navegacao">
      <nav className="nav-bar">
        <a>Painel Aluno</a>
      </nav>
    </div>
  ); 
}

export default Header;